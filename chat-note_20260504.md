# Chat note — 2026-05-04

요약: 같은 날 세션에서 나온 질문·코드 변경 사항을 정리함.

---

## 1. 일반 Q&A

### 프랑스어 이름 (G로 시작)

예: Gabriel, Guillaume, Gaspard, Gaston, Georges, Gérard, Gilles, Gaël, Gabrielle, Geneviève, Gaëlle 등 (세션에서 목록 제공).

### Render 무료 티어 (free web service)

- **슬립(스핀다운)**: 인바운드 트래픽(HTTP, 기존 WebSocket 메시지)이 **15분** 없으면 스핀다운.
- **다시 켜질 때(콜드 스타트)**: 첫 요청 후 대략 **수십 초~1분 전후**로 문서에 안내됨(앱/이미지에 따라 달라질 수 있음).
- **스핀다운 후 슬립으로 들어가는 데**: 중요한 수치는 **유휴 15분**; 실제 종료는 짧은 정리로 끝남.

---

## 2. `lang` 프로젝트 — 퀴즈/발음 UI (`en/lib/quiz-app.js` 등)

### 45초 녹음 프로그레스

- `PRONOUNCE_MAX_DURATION_MS = 45 * 1000`에 맞춰 녹음 중 **진행 막대 + 경과 시간** 표시.
- `clearPronounceRecordingProgress`, `renderPronounceRecordingProgress`, `beginPronounceRecordingProgress` 및 `en/css/quiz-style.css`의 `.pronounce-rec-*` 스타일.

### bd1만 발음 채점이 보이는 이유

- **같은 `quiz-app.js`를 써도**, 발음용 DOM(`#btnPronounceRecord`, `#pronounceStatus`, `#pronounceOutcome`, `#pronouncePanel` 등)은 **`fr/kn/bd1/index.html`에만** 들어 있음.
- 다른 `index.html`은 해당 마크업이 없어 `querySelector`가 `null` → 이벤트·표시가 동작하지 않음. 필요 시 bd1의 `pronouncePanel` 블록을 복제.

### 녹음 버튼 문구 → 아이콘

- 한글 라벨(「마이크 활성 / 마이크 비활성」) 대신 **SVG**:
  - 대기: 마이크 아이콘 (`PRONOUNCE_SVG_MIC`)
  - 녹음 중: 정지(네모) 아이콘 (`PRONOUNCE_SVG_STOP`)
- `setPronounceRecordButton(recording)` + `aria-label` / `title` 유지.
- `fr/kn/bd1/index.html` 버튼 초기 내용도 SVG로 맞춤.
- `quiz-app_무소음구간처리.js`에도 동일 패턴이 있으면 그쪽도 동기화(해당 파일은 무소음 트림 등 별도 로직).

### 서버 슬립·무응답 시 안내 문구

- 상수: `PRONOUNCE_BUSY_MESSAGE = '채점자가 바쁩니다. 3분정도 뒤에 다시 시도 해주세요'`
- `PRONOUNCE_FETCH_TIMEOUT_MS = 3 * 60 * 1000` — `fetch`에 `AbortController`로 **3분** 클라이언트 타임아웃.
- **처리 케이스**: `AbortError`, 기존 네트워크 휴리스틱(`failed to fetch` 등), HTTP **502 / 503 / 504 / 524**.
- `showPronounceServerBusy()`: `#pronounceStatus` + `toast`로 동일 문구.
- 네트워크 실패를 **「허가된 사용자가 아닙니다」**로 보내던 기존 분기는 이 시나리오용 메시지로 대체(오해 방지).

---

## 3. 관련 파일 (주로 `lang` 워크스페이스)

| 영역 | 경로 |
|------|------|
| 메인 앱 | `lang/en/lib/quiz-app.js` |
| 변형(무소음 구간) | `lang/en/lib/quiz-app_무소음구간처리.js` |
| 스타일 | `lang/en/css/quiz-style.css` |
| 발음 UI가 있는 HTML 예시 | `lang/fr/kn/bd1/index.html` |

---

## 4. 서버 쪽

이 노트는 **learnlang_server**에 저장. 실제 API 베이스 URL 등은 `quiz-app.js`의 `PRONOUNCE_API_BASE`(예: Render)에 정의됨.
